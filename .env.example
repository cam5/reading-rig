# Copy to .env. Prisma reads this via prisma.config.ts (which imports dotenv).
# A local SQLite file — the library and the commonplace book are one file you own.
DATABASE_URL="file:./dev.db"

# The Rig, from M3. ANTHROPIC_API_KEY is yours to set by hand — in deployed
# environments it's a Railway environment variable per environment, not
# committed here. The other two are written by `npm run agent:setup`
# (scripts/setup-agent.ts), which creates the Managed Agent once and
# converges it in place on every re-run (skipping the update if nothing
# changed); the agent is never created in the request path. scripts/release.ts
# also runs this on every deploy, when ANTHROPIC_API_KEY is set for that
# environment.
# ANTHROPIC_API_KEY=
# READING_RIG_AGENT_ID=
# READING_RIG_AGENT_VERSION=

# app/routes/rig.tsx (#26) needs a Managed Agents environment to create a
# session against — every session provisions a container workspace, even
# for an agent like the Rig that only uses custom tools plus web
# search/fetch. Provisioning one is not yet scripted (no equivalent of
# `agent:setup` exists for environments); create one by hand for now and
# paste its id here.
# READING_RIG_ENVIRONMENT_ID=
